# ── Web Framework ───────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
jinja2>=3.1.2,<3.1.5              # 3.1.5+ breaks Gradio template caching (unhashable dict)

# NOTE: gradio and torch are pre-installed by Hugging Face Spaces — do NOT list them here

# ── Hugging Face ────────────────────────────────────────────────────────
huggingface_hub>=0.25.0,<1.0.0   # gradio 4.x needs HfFolder which was removed in 1.0
datasets[audio]>=3.0.0

# ── TTS Engines ─────────────────────────────────────────────────────────
# coqui-tts>=0.24.0               # Disabled: pulls PyTorch (~300MB+), not used in production (only edge_tts is active)
edge-tts>=6.1.0                   # Microsoft Edge neural TTS (cloud, no API key)
gTTS>=2.5.0                       # Google Text-to-Speech (lightweight cloud TTS)
# bark: optional, install separately:
#   pip install git+https://github.com/suno-ai/bark.git

# ── Audio Processing ───────────────────────────────────────────────────
soundfile>=0.12.1
pydub>=0.25.1                     # requires ffmpeg on system PATH
numpy>=1.26.0

# ── Config / Secrets ───────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Google Gemini AI ───────────────────────────────────────────────────
# google-generativeai>=0.8.0      # Removed: using direct REST API instead (saves ~50MB, avoids deprecation warnings)

# NOTE: torch is pre-installed by HF Spaces — only uncomment for local/Render/Cloud Run:
# torch>=2.0.0

